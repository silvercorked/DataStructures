#pragma once

/*
namespace DataStructures {
	template <typename E>
	struct Iterator : public E* {
		Iterator<E>& advance(Iterator<E> it, unsigned int d) {
			it
		};
		Iterator<E>& distance();
		Iterator<E>& begin();
		Iterator<E>& end();
		Iterator<E>& prev();
		Iterator<E>& next();
	};
}
*/
