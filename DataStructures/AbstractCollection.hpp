#pragma once

/*
#include "Iterator.hpp"

namespace DataStructures {
	template <typename E>
	class AbstractCollection {
		
		public:
			virtual Iterator iterator() = 0;
			virtual int size() = 0;
			virtual bool isEmpty();
			virtual bool add(const E& item);
			virtual bool addAll(const AbstractCollection<E>& items);
			virtual void clear();
			virtual bool contains(const E& item);
			virtual bool containsAll(const AbstractCollection<E>& items);
			
			
			virtual bool remove(const E& item);
			virtual bool removeAll(const AbstractCollection<E>& items);
			virtual bool retainAll(const AbstractCollection<E>& items);
			
			virtual E[] toArray();
	};

	template<typename E>
	inline bool AbstractCollection<E>::isEmpty() {
		return this->size() == 0;
	}

}

*/
